name: 'AI Review with Gitea Inline Support'
author: 'dungtranhoang'
branding:
  icon: 'eye'
  color: 'blue'
description: '🚀 AI-powered code review with improved Gitea inline comments and JSON parsing'

inputs:
  review-command:
    default: 'run'
    required: false
    description: 'Which review mode to run (run, run-inline, run-summary, run-context, run-inline-reply, run-summary-reply)'

runs:
  using: 'docker'
  image: 'Dockerfile'
